# Future home of: environment loading, validation, config schema
# Current locations: env vars read ad-hoc via os.environ across multiple modules
# See config/.env.example at repo root for all supported env vars
# Do NOT migrate code here without explicit instruction. See .ai/AIR_RULES.md
