[modules]
autoload = yes

; Ensure these are loaded
load = res_pjsip.so
load = res_pjsip_session.so
load = chan_pjsip.so
load = app_read.so
load = app_saydigits.so
load = app_playback.so
load = app_wait.so
load = pbx_config.so
load = func_math.so
