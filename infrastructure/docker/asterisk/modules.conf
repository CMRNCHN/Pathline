[modules]
autoload = yes

; Ensure these are loaded
load = res_pjsip.so
load = res_pjsip_session.so
load = chan_pjsip.so
load = app_read.so
load = app_saydigits.so
load = app_playback.so
load = app_wait.so
load = pbx_config.so
load = func_math.so

; ── ARI / Stasis stack (Pathline Pulse) ──────────────────────────────────────
; autoload=yes already pulls these in; listed explicitly so an ARI-capable
; container is self-documenting and survives a future autoload=no.
load = res_http_websocket.so
load = res_ari.so
load = res_ari_channels.so
load = res_ari_events.so
load = res_stasis.so
load = app_stasis.so
load = func_talkdetect.so
